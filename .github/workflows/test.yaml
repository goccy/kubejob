name: test
on: [pull_request]
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
    - name: checkout
      uses: actions/checkout@v2
    - name: Setup kind
      run: |
        make deploy
    - name: Run test
      run: |
        timeout 120 make test
